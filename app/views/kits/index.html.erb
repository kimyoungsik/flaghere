<h1>Listing kits</h1>

<table>
  <tr>
    <th>User</th>
    <th>Content</th>
		<th>kitable_type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @kits.each do |kit| %>
  <tr>
    <td><%= kit.user_id %></td>
    <td><%= kit.content %></td>
		<td><%= kit.kitable_type %></td>
    <td><%= link_to 'Show', kit %></td>
    <td><%= link_to 'Edit', edit_kit_path(kit) %></td>
    <td><%= link_to 'Destroy', kit, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Kit', new_kit_path %>
